{
  "type": "module",
  "main": "./build/index.js",
  "scripts": {
    "build": "node esbuild.js",
    "start": "concurrently --raw \"node esbuild.js\" \"func start --verbose --enableDebug\" "
  },
  "devDependencies": {
    "@azure/functions": "^4.5.1",
    "esbuild": "^0.24.0",
    "concurrently": "^9.0.1"
  }
}
